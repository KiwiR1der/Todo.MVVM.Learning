<Window x:Class="TodoList.MVVM.ToolKit.Views.EditTodoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:TodoList.MVVM.ToolKit.Views"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="EditTodoWindow"
        Width="400"
        Height="320"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />

        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"
                   Text="标题：" />
        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,0,0,8"
                 hc:InfoElement.Title="标题"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"
                   Text="截止日期：" />
        <hc:DateTimePicker Grid.Row="1"
                           Grid.Column="1"
                           Margin="0,0,0,8"
                           SelectedDateTime="{Binding DueDate, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />

        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"
                   Text="状态：" />
        <CheckBox Grid.Row="2"
                  Grid.Column="1"
                  Margin="0,0,0,8"
                  Content="已完成"
                  IsChecked="{Binding IsDone, Mode=TwoWay}" />

        <CheckBox Grid.Row="3"
                  Grid.Column="0"
                  Content="开启提醒"
                  IsChecked="{Binding ReminderEnabled}" />

        <TextBox Grid.Row="3"
                 Grid.Column="1"
                 hc:InfoElement.Title="提前提醒时间（分钟）"
                 hc:InfoElement.TitlePlacement="Top"
                 Style="{StaticResource TextBoxExtend}"
                 Text="{Binding ReminderBeforeMinutes}" />

        <UniformGrid Grid.Row="4"
                     Grid.ColumnSpan="2"
                     Margin="5"
                     Columns="2">
            <Button Click="Button_Click"
                    Command="{Binding OKCommand}"
                    Content="确定"
                    Cursor="Hand"
                    IsDefault="True"
                    Style="{StaticResource ButtonPrimary}" />
            <Button Content="取消"
                    Cursor="Hand"
                    IsCancel="True"
                    Style="{StaticResource ButtonPrimary}" />
        </UniformGrid>
    </Grid>
</Window>
